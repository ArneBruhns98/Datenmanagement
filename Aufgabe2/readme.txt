DB-Access is set to test@localhost identified by password which is password,
there needs to be an empty database called 'gebrauchtwagen'. Lombok needs to be added to run from source in IDE.
Tests in Postman may only be run once, because they change the state of db content, while checking for inconsistency.
